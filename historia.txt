comandos para seguir con mis pruebas en git

touch nombrearchivo.extension//sirve para crear archivo

clear //para limpiar ventana o con ctrl+c

mkdir nombredecarpeta //sirve para crear carpeta o directorio nuevo

dir //muestra archivos dentro de la carpeta en la que se encuentra es igual a ls

Esc+shift+z+z //sirve para salir de la ventana

git rm arvhicoaremover.extension //sirve para remover el archivo que queramos

git log //se muestra todo el historial

git log --all --graph -decorate --oneline //muestra el historial detallado y mas compactado para su lectura
alias nombrequequeramos="git log --all --graph -decorate --oneline"//sirve para almacenar toda esa linea en una sola palabra y poder de manera mas facil el arbol realizado hasta ahora donde se ve el main y sus ramas
nombrequequeramos//luego de nombrar solo se escribe el nombre asignado y enter, nos mostrará todo como si hubiesemos escrito toda la linea

git log archivo.txt //muestra el historial de cambios del archivo
history //sirve para ver el historial de comandos usados
!numero //se indica el numero del comando usado segun lo que indique el historial de comandos usados
aca se inicia todo:

pwd //sirve para saber en que carpeta me encuentro ubicado

git init //para crear una carpeta temporal en nuestro proyecto que indica que se trabajara con git

git config //se muestra la configuracion actual del git
el pull y fetch //sirve para traer la info del github
push //sirve para enviar la informacion al github

ls -al //sirve para mostrar la informacion que hay en la carpeta donde estemos

git status //indica el status actual de los archivos dentro de la carpeta
git add . //se agrega la documentacion hacia el temporal para luego realizar el commit
git commit -m "estoesuncomentario" //esto sirve para realizar un commit y se agrega un comentario

git commit -am "estoesuncomentario" //esto sirve para realizar un commit, la a indica que siempre se agregue, pero solo se cumple cuando anteriormente al archivo ya fue agregado (se uso add .), pero si no se ha usado antes no tendrá efecto

git status
git cat nombredelarchivo.txt //sirve para visualizar contenido del archivo
git branch esteeselnombredelbranch //sirve para crear una rama(branch) va a estar aparte, no toca el main
git checkout esteeselnombredelbranch //sirve para cambiar a un branch o al main
git branch //lista las ramas existentes
git merge esteeselnombredelbranch //sirve para fusionar el master con una rama, se debe estar en el main para no perder los datos del main, se perdera los datos de la rama que se indique

para conectar git con github:
git remote add origin aquiponeurldelgithub //se agrega el enlace hacia el repositorio de github
git remote //sirve para saver si esta en el origin
//antes de seguir, verificar con git status si hay cambios, luego aplicar git commit -am "mensaje" para que pueda ser reconocido al momento de hacer el push sino no lo reconocera, tambien si en caso no funciona se debe usar el git add . o el git add nombrearchivo.extension, esto se hace si en caso con anterioridad no se realizo el agregado del archivo o archivos al temporal del git.
git pull origin main //trae la informacion del github hacia nuestra carpeta local
git pull origin main --alow-unrelated-histories //sirve para unir con el github, por si el anterior comando muestra error
git push origin main //para subir la informacion al github

crear llave ssh:
ssh-keygen -t rsa -b 4096 -C "correodegithub@hotmail.com" //para generar clave ssh, luego indica donde se quiere guardar la clave, pero se deja donde esta, ya que es la carpeta donde estoy usando mi proyecto y se da enter, luego pide otra clave, se coloca clave. se genera archivo id_rsa

eval $(ssh-agent -s)// se da enter y signifa que esta activando el proceso del ssh
//ir a la carpeta por defecto en este caso se esta en la carpeta cloud
ssh-add ~/.ssh/id_rsa//sirve para agregar la llave privada al github
//ir y verificar la llave publica (la que termina con pub)) y abrir con vscode, copiar todo, ir a github, ir a settings, ir a ssh and gpg, crear ssh, poner titulo y pegar la llave copiada.
//ahora ir a el proyecto donde se esté trabajando, copiar llave ssh donde dice clone or download

pasos luego de realizar la clave ssh:

//ir primero a la carpeta del proyecto
git remote -v //para visualizar la existencia del origin
git remote set-url origin setraelaclavesshgeneradaengithub //con esto se logra la vinculación con github
git remote -v //para visualizar la existencia del origin y que se realizo el cambio
//hacer prueba y realizare cualquier cambio
//primero se hace un pull y luego el push para realizar la prueba
git pull //escribir yes
git pull origin main //se da enter para traer lo de github al local para combinar
git status //para ver si hay cambios
git commit -am "escribirmensaje"
git push origin main //para enviar la info al github

para hacer etiquetas:

git tag -a v.01 -m "resultado de las primeras"//para añadir un tag o etiqueta
git tag //para ver todas las tag
git show-ref --tags //para saber que hash esta asginado a cada tag
git pull origin main//traer todo de github
git push origin --tags//para realizar el envio de los tags

si se desea eliminar un tag:
git log --all --graph -decorate --oneline//o en todo caso llamar a su alias asiganda, ya que esto sirve para ver el hash asignado a cada commit(el cod asignado son los primeros digitos mostrados al inicio de cada linea)

git tag - a quieroeliminar -m "esto es lo que elminiare" aquisecopiaelhashdelarbol 
git pull origin main
git push origin main --tags
git tag //para ver los tags existentes
git tag -d nombredetag //la d de delete
git status
git pull origin main
git push origin master//al ir a la pagina sigue ahi pero en el local ya no esta
git push origin :refs/tags/nombredeltag //con esto se borra el tag en el github(la pagina)


git show-branch --all//sirve para ver el historial por rama
gitk //abre en una ventana aparte la historia de las ramas y tags mas detallados, es preferible usar la consola
git branch//sirve para listar ramas
//dentro de main se puede realizar el envio de las ramas que haya creado, ejem: git push origin header, git push origin footer, esto sirve para que se peudan visualizar las ramas en la pagina.

si traigo un proyecto nuevo con otro usuario:

git clone urlhttpsdegithub //sirve para clonar un proeycto de github

//se hace cambios a cualquier archivo, luego se hace el git status, luego git commit -am "mensaje", luego el git pull origin main, luego origin push origin main, pero aparecera un problema por temas de permisos(esto se debe a que el propietario del proyecto no otrogo permisos), esto se soluciona cuando el propietario va a su cuenta de github, ingresa a su proyecto y va a settings, ir a manage access, se invita a colaborador ingresando su correo o tambien se puede poner su username (portuay)

//Basicamente el flujo de trabajo seria como:
//Crear ramas
//Asignar una rama a cada programador
//El programador baja el repositorio con git pull origin master
//El programador cambia de rama
//El programador trabaja en esa rama y hace commits
//El programador sube su trabajo con git push origin #nombre_rama
//El jefe baja y unifica todos los cambios


